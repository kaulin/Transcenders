services:
  api:
    build:
      context: .
    command: sh -c "npm ci --include=dev && npm run dev"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    ports:
      - "3000:3000"
volumes:
  node_modules:
