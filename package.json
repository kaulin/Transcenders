{
  "name": "transcenders",
  "private": true,
  "workspaces": [
    "packages/*",
    "services/*",
    "web"
  ],
  "version": "1.0.0",
  "description": "transcenders",
  "author": "",
  "license": "ISC",
  "scripts": {
    "dev:all": "concurrently --names 'USER,AUTH,FRONTEND' --prefix-colors 'blue, purple, green' 'npm run dev --workspace=user-service' 'npm run dev --workspace=auth-service' 'npm run dev --workspace=web'",
    "dev:backend": "npm run dev --workspace=user-service",
    "dev:frontend": "npm run dev --workspace=web",
    "test": "tsx scripts/tests.ts",
    "api-client-test": "tsx scripts/api-client-test.ts"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "sql-formatter-plus": "^1.3.6"
  }
}
